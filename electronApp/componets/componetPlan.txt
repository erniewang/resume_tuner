

CustomContainer(vertical, bgC, fracParent) -> a basic rectangulat componet.
    cases: 
        if vertical is true:
            w-full, height would be specified or be fracParent.
            flex row. like pancakes for children
        else:
            h-full, width would be the changable factor.
            flex-col like bookshelves for children
    fracParent would be any number from 1 to 6. because there will be a mapping that links to w-1/6 , w-2/6, or h-5/6 
    or some shit like that. can be used because container might be child of another contaier
    bgC: the backgrond

CustomButton EXTENDS all properties of container , but also has "clickFunction" and name: 
    -> so verticality is needed because it needs to know if the parent element is vertical or horizontal so it can size
    itself accordingly.
    -> clickfunction will be some function drilled there by some parent/ancestor that be utilized for functionality
    -> special active properties. 
    name will just be what the buttons says about shit


CustomInput EXTENDS properties of container, has a inputType, and description. 
    -> inputType speicies what type of input, checkbox? input? or whatever
    -> description. a descripion or a name that will indicate what the input is for
    -> flex and within contains the real input and the description